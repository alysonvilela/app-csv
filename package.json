{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist"
  },
  "dependencies": {
    "fast-csv": "^5.0.1",
    "hono": "^4.4.6",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
